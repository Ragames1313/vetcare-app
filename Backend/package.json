{
  "name": "vetcare-backend",
  "version": "1.0.0",
  "description": "API REST sencilla para VetCare.",
  "main": "src/server.js",
  "scripts": {
    "test": "jest --testMatch \"**/tests/**/*.test.js\"",
    "test:unit": "jest --testMatch \"**/tests/**/*.test.js\"",
    "test:integration": "node tests/api.integration.js",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mariadb": "^3.4.0"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
